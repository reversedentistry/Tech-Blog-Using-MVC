<div class="container-md">
    <div class="d-flex justify-content-between bg-info text-dark">
    <h4 class="p-2"><a href="/post/{{id}}">{{title}}</a></h4>
    <p class="p-2">Posted by {{user.username}} on {{format_date date_created}}</p>
    </div>
    <div class="border border-3">
        {{post_text}}
    </div>
</div>