{{#if posts.length}}
{{#each posts as |post|}}
<div class="post-name row">
    <a href="/post/{{id}}">{{title}}</a>
    <p>{{date_created}}</p>
</div>
{{/each}}
{{/if}}

<button type="button" class="btn btn-info">+ New Post</button>